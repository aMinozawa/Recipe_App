<p><%= @recipe.title %></p>
<p><%= image_tag @recipe.dish.url if @recipe.dish? %></p>
<p><%= @recipe.material %></p>
<p><%= @recipe.recipe %></p>
<% if @recipe.user_id == current_user.id %>
  <%= link_to("編集", "/recipes/#{@recipe.id}/edit") %>
  <%= link_to("削除", "/recipes/#{@recipe.id}", method: :delete, data: {confirm: "本当に削除しますか？"}) %>
<% end %>
<div>
  <%= form_with(model: [@recipe, @comment], local: true) do |f| %>
    <div>
      <%= f.label :body, "コメント" %><br>
      <%= f.text_area :body %>
    </div>
    <%= f.submit %>
  <% end %>
</div>
<hr>
<div>
  <% @recipe.comments.each do |comment| %>
    <p><%= comment.body %></p>
  <% end %>
</div>